class Solution {
public:
    int data;
    nofde*next;
    node*prev;
    
    node(int d){
        this->data=d;
        this->next=NULL;
        this->prev=NULL;
    }
}

void square(node*&head,node*&tail){

    node*prev=head;
    node*curr=head->next;
while(temp!=NULL){
    node*temp=curr->next;
    prev=curr;
    curr=temp;
    
}
    curr->next=head;
}

int main(){
    node*head=new node(d);
    
}